# 部署说明 —— 记账 · 对账单 PWA

## 产物

`记账对账单-PWA-v1.0.zip`（解压后为 `dist/` 目录内容），纯静态文件，无后端、无构建步骤。
全部资源为相对路径，可部署在域名根目录或任意子路径下。

## iOS 安装步骤

1. 把静态文件托管到 **HTTPS** 地址（必须，Service Worker 与「添加到主屏幕」都要求安全上下文；本机调试用 localhost 也可）；
2. iPhone 上用 **Safari** 打开该网址（必须是 Safari，微信内置浏览器不支持安装 PWA）；
3. 点底部分享按钮 → 「**添加到主屏幕**」→「添加」；
4. 主屏幕出现「记账对账单」图标，点开即全屏独立运行，**离线可用**（数据存在本机 IndexedDB）。

## 零成本托管方式（任选其一）

### GitHub Pages

1. github.com 新建一个公开仓库，如 `accounting-pwa`；
2. 解压 zip，把全部文件提交到仓库根目录（或 `docs/` 目录）：
   ```bash
   git init && git add . && git commit -m "deploy"
   git remote add origin https://github.com/<你的用户名>/accounting-pwa.git
   git push -u origin main
   ```
3. 仓库 Settings → Pages → Source 选 `main` 分支 `/ (root)`，保存；
4. 约 1 分钟后访问 `https://<你的用户名>.github.io/accounting-pwa/`（自带 HTTPS，子路径部署本应用已兼容）。

### Vercel

1. vercel.com 用 GitHub 账号登录（或邮箱注册）；
2. 「Add New → Project」导入上述仓库，Framework 选 **Other**，不填任何构建命令，直接 Deploy；
3. 得到 `https://<项目名>.vercel.app` 地址。
   也可不建仓库：安装 `npm i -g vercel` 后在解压目录执行 `vercel --prod` 直接上传。

### Netlify

1. app.netlify.com 注册登录；
2. 「Add new site → Deploy manually」，把解压后的**文件夹整体拖进**网页虚线框；
3. 立即得到 `https://<随机名>.netlify.app`（可在 Site settings 改名）。

## 注意事项

- **必须 HTTPS 或 localhost**：HTTP 域名下 Service Worker 不生效，无法离线、无法安装到主屏幕（页面本身仍可用）。
- **iOS 数据清理风险**：数据只存本机 IndexedDB。若站点长期（数周）未被打开，iOS 可能清除未添加到主屏幕站点的存储；安装到主屏幕后可显著降低该风险。换机/清理 Safari 数据会丢失记录，重要对账单请及时导出 PDF 留存。
- **更新代码后必须 bump 缓存版本**：修改任何 js/css/html 后，把 `sw.js` 里的 `CACHE` 版本号（如 `accounting-v3` → `accounting-v4`）加一，否则已安装的用户会一直拿到旧缓存。
- 桌面浏览器（Chrome/Edge）同样可用，导出 PDF 为下载方式；iOS 上「导出/分享 PDF」走系统分享面板，可直接发微信或打印。
