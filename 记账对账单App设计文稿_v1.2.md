# 记账 · 对账单 App 设计文稿

> 版本：v1.2  日期：2026-09-09
> 定位：面向小微企业、工地、供应商的轻量记账出单工具——快速记录供货/加油流水，一键生成可打印、可微信分享的对账单。
>
> **v1.2 修订说明**
> 1. 账单集不再绑定统计方式：新建只需填名称；「按日期 / 按车辆」降为纯显示层与输出层的选择，随时可切；
> 2. 账目页分组列表增加「排布方式」切换（按日期 / 按车辆），仅影响显示；
> 3. 对账单生成时选择「输出格式」（流水表 / 聚合双栏表），列设置跟随所选格式；
> 4. 数据模型移除 `mode` 字段——两种视角本就是同一张流水表的两个 `GROUP BY`，数据层不变。
>
> **v1.1 修订说明**
> 1. 金额改为整数运算（数量存千分之一升、单价存分），杜绝浮点误差；
> 2. 车辆输入改为「历史补全 + 允许新建」，并支持同名批量替换，保证按车辆聚合正确；
> 3. 明确就地编辑边界：仅文书性文字可就地改，明细数字一律回流水修改；
> 4. 单位收敛为「升」，移除单位选择；
> 5. 预览口径明确为「近似布局，以导出 PDF 为准」；
> 6. 账期不做筛选，由说明文字就地修改；
> 7. 交互全面移动端化：去除 hover 与长按，删除统一为「左滑 + 确认弹窗」；
> 8. 术语、确认弹窗、按钮层级全文统一。

---

## 1. 产品概述

### 1.1 核心价值
把每天的供货/加油记录快速记下来，按账单集归档，月底一键生成纸质风格的对账单，直接打印或微信发给对方确认。

### 1.2 设计原则
- **轻量**：底部三栏（账目 / 记一笔 / 对账单），无冗余功能，离线可用；新建账单集只需填名称。
- **快**：连续记账一键接着一笔记；金额自动 = 数量 × 单价，整数运算，无分厘误差。
- **所见即所得**：对账单的文书性文字就地点击编辑；预览用于核对布局与文字，最终效果以导出 PDF 为准。

### 1.3 范围界定（本期不做）
多单位（本期仅「升」）、OCR 识票、云端备份、报表统计、欠款管理、账期筛选。导出为 PDF，经系统分享面板发送（可选微信、打印等）。

---

## 2. 核心概念与术语

全文统一使用三个术语，不使用其他叫法：

| 术语 | 说明 |
|---|---|
| **账单集** | 自定义分组（项目 / 时间段 / 客户），记账和出单的基本单位；只有名称，无其他属性 |
| **条目** | 一笔流水：日期、加油车辆、数量（升）、单价（元/升）、备注，归属一个账单集 |
| **对账单** | 由一个账单集生成的导出文档（PDF） |

**两种视角**：同一张流水表支持「按日期分组」和「按车辆分组」两种视角（`GROUP BY date` / `GROUP BY vehicle`），均为**显示层 / 输出层选择，随时可切，与数据无关**：
- 账目页称「**排布方式**」：决定分组列表怎么显示；
- 对账单页称「**输出格式**」：决定生成流水表还是聚合双栏表（见 §5）。

---

## 3. 信息架构

```
账目（首页）
├── 账单集卡片条（横向滑动）
│   ├── ＋ 新建账单集（固定首位）
│   └── 账单集卡片：点按选中 / 铅笔→编辑弹窗（弹窗内含删除）
├── 排布方式切换（分段控件：按日期 / 按车辆，仅影响显示）
├── 分组列表（随排布方式分组）
│   ├── 组头：主键名（加粗）+ 右侧 N笔 · 合计金额
│   └── 条目行：点按→详情页 / 左滑→删除（确认弹窗）
│       ├── 详情页：字段卡片 + 底部操作条（左删除红 / 右编辑）
│       └── 编辑页：回填表单，保存返回
记一笔
└── 所属账单集 → 日期 → 数量（升） → 单价 → 加油车辆（补全） → 备注 → 金额预览 → 保存
对账单
└── 选择账单集 → 输出格式选择（按日期流水表 / 按车辆聚合表）→ [一键生成对账单] [列设置]
    └── 生成后：纸张预览（文书性文字就地编辑）→ 导出/分享 PDF
```

**全局删除规范（统一）**：所有删除均先弹确认弹窗，文案统一为「删除后不可恢复，确认删除？」。入口共两种，按场景固定：
- 列表条目（账目页条目行）：**左滑出现删除按钮**；
- 账单集：删除入口统一放在**编辑弹窗底部红色按钮**（卡片不提供长按/左滑，避免横向滑动冲突）。

---

## 4. 页面设计

### 4.1 账目页（首页）

**账单集卡片条**（横向滑动，置顶）
- 「＋ 新建账单集」固定在最左，账单集再多也不用滑到底。
- 卡片结构：主行 = 名称（两行截断）；副行 = N笔 · 合计金额（灰色小字）。信息不挤压名称。
- 交互：**点按卡片 = 选中**（高亮描边）；**铅笔图标 = 编辑**（弹窗）。
- 删除：仅在编辑弹窗内，红色「删除此账单集」，确认弹窗提示将级联删除该集全部条目；**至少保留一个账单集**，仅剩一个时删除按钮置灰。

**新建 / 编辑账单集弹窗**（两个入口共用）
- 仅一个名称输入框，**不再选择统计方式**，创建即完成。
- 编辑模式下底部显示红色「删除此账单集」。

**排布方式切换**
- 分组列表上方分段控件：**按日期 ｜ 按车辆**，仅改变当前列表的分组显示，不影响任何数据。
- 选择按账单集记忆，下次选中该集时沿用上次排布。

**分组列表**（跟随排布方式）
- 每个分组为独立卡片：**组头左加粗主键名 + 右侧灰色「N笔 · 合计金额」**。
- 条目行层级：主行加粗（按日期排布显示车辆名；按车辆排布显示日期），副行灰色小字（数量 × 单价 · 备注），右侧金额加粗。**主重副轻**。
- 同一分组内条目按录入时间排序（`created_at`），保证顺序稳定。

**条目操作**
- 点按条目 → 详情页：字段卡片（日期 / 车辆 / 数量（升） / 单价 / 金额 / 备注）+ 底部左右分栏操作条（**左「删除」红色，右「编辑」**）。
- 编辑页：表单回填，保存返回列表；**可修改所属账单集**（下拉换集）。
- 左滑条目 → 删除按钮 → 确认弹窗。

### 4.2 记一笔

字段顺序（自上而下）：
1. 所属账单集（下拉，**默认记住上次选择**，可随时更换）
2. 日期（默认今天，记住上次选择）
3. 数量（升，最多三位小数）
4. 单价（元/升，两位小数，记住上次输入）
5. 加油车辆（**文本输入 + 历史补全**，见 §4.2.1）
6. 备注（可选）
7. 金额实时预览（数量 × 单价，只读，显示规则见 §6.2）
8. 底部双按钮：[**保存这笔**]（主）｜[**保存并再记一笔**]（次）

**保存行为**：
- 「保存这笔」：跳回账目页并定位到该账单集；
- 「保存并再记一笔」：停留在本页，保留账单集与日期，清空数量/车辆/备注，Toast 提示「已保存」——适配一个工地一天连续加多台车的场景。

#### 4.2.1 车辆输入规范（保证聚合正确）

按车辆视角的核心是 `GROUP BY vehicle`，自由文本会产生「3号挖机」「3 号挖机」被拆成两台车的问题，必须约束：

- 车辆输入框**带自动补全**：输入时实时下拉匹配**当前账单集内已有车辆名**（去重列表），点选即填入；也允许直接输入新名称。
- 编辑条目修改车辆名时，若该集内存在同名旧值，弹窗二选一：「**仅改此笔** / **批量替换该账单集内所有同名车辆**」。

### 4.3 对账单页

流程：**选择账单集 → 选择输出格式 → 一键生成 → 就地编辑 → 导出/分享**。

1. 顶部：账单集选择下拉。
2. **输出格式选择**（分段控件，二选一，默认按日期流水表，选择按账单集记忆）：
   - **按日期流水表**：逐笔明细，版式见 §5.1；
   - **按车辆聚合表**：聚合双栏，版式见 §5.2。
3. 一行并排 [**一键生成对账单**（主按钮，占满）｜**列设置**]。
4. 点击生成后出现纸张预览，版式复刻纸质对账单：
   - 标题（居中，可编辑）
   - 说明文字（按模板自动生成，如「XX项目，2026年6月1日至6月30日，共加注柴油 24916.58 升，合计 170463.99 元。」；日期范围取该集条目的最早~最晚日期，**全文可就地修改**，账期不对时由使用者直接改文字，本期不做日期筛选）
   - 供方、出单日期（右对齐，可改；**供方首次填写后全局记忆**，之后自动带出）
   - 明细表格（版式随所选输出格式，见 §5）
5. **就地编辑边界（核心规则）**：
   - 可就地编辑：**标题、说明文字、供方、出单日期**等文书性内容。修改仅作用于当前对账单，**不回写流水数据**；修改结果按账单集记忆，下次生成时作为默认值带出。
   - 明细表格数字**不可就地修改**。点按明细区域时 Toast 提示「数字来自流水，请到账目页修改后重新生成」。金额确需调整（如抹零）本期不支持，列入后续规划。
6. 可编辑区域**常显虚线框 + 铅笔角标**（移动端无 hover，不用悬停提示），点按即进入编辑态。
7. **预览口径**：预览用于核对布局与文字内容，排版细节（字体、行距、线宽）以导出 PDF 为准，不承诺像素级一致。
8. 导出：[**导出 / 分享 PDF**] 全宽按钮，调起系统分享面板（可选微信发送、打印、存储等），不承诺直达微信。

### 4.4 列设置弹窗

- 入口在对账单页与「一键生成」并排。
- **列方案跟随当前所选输出格式**，各一套预设列（单位固定为升）：
  - 按日期流水表：序号 / 日期 / 车辆 / 升数（升） / 单价（元） / 金额（元）
  - 按车辆聚合表：车辆 / 金额（元） / 升数（升）
- 每列支持：勾选输出、自定义列名、↑↓ 调整顺序。按车辆聚合表为双栏，列配置对左右两栏同时生效。
- 列名同样可在纸张表头上直接改。
- **列配置按「账单集 + 输出格式」分别持久化记忆**（明确需求，非建议）。

---

## 5. 对账单双版式（核心）

两种版式均由使用者在生成时选择，与账单集无绑定。数字显示规则统一：金额两位小数（元）；升数最多三位小数、去除末尾零（2520 显示「2520」，2645.570 显示「2645.57」）。

### 5.1 按日期流水表——逐笔明细

| 序号 | 日期 | 车辆 | 升数（升） | 单价（元） | 金额（元） |
|---|---|---|---|---|---|
| 1 | 7月1日 | 3号挖机 | 663 | 6.90 | 4574.70 |
| … | | | | | |
| **合计** | | | **6435** | | **42904.50** |

### 5.2 按车辆聚合表——聚合双栏

同一车辆的多笔加油先聚合（数量求和、金额求和），按车辆名称排序，再**左右双栏排版**（左 3 列一辆车、右 3 列一辆车，奇数辆时右下留空），对应纸质原版式：

| 车辆 | 金额（元） | 升数（升） | 车辆 | 金额（元） | 升数（升） |
|---|---|---|---|---|---|
| 1号挖机 | 793.50 | 115 | 3号挖机 | 17275.00 | 2520 |
| 5号挖机 | 18143.03 | 2645.57 | 6号挖机 | 19481.30 | 2843 |
| … | | | | | |
| **合计**（跨两列） | **总金额**（跨两列） | **总升数**（跨两列） | | | |

合计行占满整行、与表头对称：每格跨两列，依次为 **合计 ｜ 总金额 ｜ 总升数**。

---

## 6. 数据模型与金额规则

### 6.1 表结构

```sql
CREATE TABLE bill_sets (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  name       TEXT NOT NULL,           -- 账单集名称（唯一属性）
  created_at INTEGER NOT NULL         -- 创建时间戳
);

CREATE TABLE entries (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  set_id     INTEGER NOT NULL REFERENCES bill_sets(id) ON DELETE CASCADE,
  date       TEXT NOT NULL,           -- YYYY-MM-DD
  vehicle    TEXT NOT NULL,           -- 加油车辆（补全输入，保证聚合正确）
  qty_ml     INTEGER NOT NULL,        -- 数量，单位：0.001 升（千分之一升）
  price_fen  INTEGER NOT NULL,        -- 单价，单位：分/升（0.01 元）
  note       TEXT DEFAULT '',
  created_at INTEGER NOT NULL         -- 录入时间戳，同分组内排序依据
);

-- 两种视角即两个查询，数据层不变：
-- 按日期：SELECT date AS key, ... FROM entries WHERE set_id=? GROUP BY date
-- 按车辆：SELECT vehicle AS key, SUM(qty_ml), SUM(qty_ml*price_fen/1000) ... GROUP BY vehicle
```

单位固定为「升」，不存 unit 字段；账单集不存统计方式，视角切换不产生任何数据迁移。

### 6.2 金额规则（杜绝浮点误差）

- **金额不落库**，一律查询时由整数运算得出：
  `金额(分) = qty_ml × price_fen ÷ 1000`，四舍五入到分。
- 全程整数运算，不使用浮点。示例校验：663 升 × 6.90 元 = `663000 × 690 ÷ 1000 = 457470 分 = 4574.70 元`，与客户手算完全一致。
- **合计 = 各行金额（分）先累加、再格式化**，保证合计与逐行之和分毫不差；升数同理（千分之一升累加后格式化）。
- 显示规则：金额固定两位小数；单价固定两位小数；升数最多三位小数、去除末尾零。

---

## 7. 技术选型与导出链路

| 模块 | 方案 |
|---|---|
| 框架 | Flutter（一套代码双端） |
| 本地数据库 | SQLite（sqflite / drift），离线可用 |
| 对账单渲染 | Dart `pdf` 插件按 §5 两种版式绘制；**中文字体需内嵌字体文件（子集化控制包体积）**，默认字体不含 CJK，动工前须先行验证 |
| 预览 | Flutter 近似排版预览，仅用于核对布局与文字，最终效果以 PDF 为准（两套渲染不承诺像素一致） |
| 导出分享 | 生成 PDF 临时文件 → 系统分享面板（`share_plus`）→ 用户自选微信/打印/存储；Android 走 FileProvider，无需微信 SDK |

**动工前竖切验证**：记一笔 → 按车辆聚合 → 生成含中文字体的 PDF → 系统分享面板发出。一次性覆盖最大技术风险（字体、金额精度、分享链路）。

---

## 8. 交互清单（验收用）

**账单集**
- [ ] 新建入口固定首位；弹窗仅名称输入框，创建即完成（无统计方式选择）
- [ ] 编辑弹窗底部红色删除（确认弹窗，级联删除条目）；至少保留一个账单集，仅剩一个时删除按钮置灰
- [ ] 卡片点按选中 / 铅笔编辑；名称两行截断、副行显示「N笔 · 合计金额」

**账目列表与条目**
- [ ] 排布方式分段切换（按日期 / 按车辆）生效，仅影响显示；选择按账单集记忆
- [ ] 分组卡片：组头加粗 + 右侧「N笔 · 合计金额」；条目主重副轻
- [ ] 同分组内条目按录入时间排序，顺序稳定
- [ ] 条目：点按→详情（底部左删右编）；左滑→删除（确认弹窗）；编辑页回填保存、可更换所属账单集
- [ ] 编辑条目改车辆名时，提供「仅改此笔 / 批量替换同名车辆」

**记一笔**
- [ ] 字段顺序正确；账单集/日期/单价记住上次输入
- [ ] 车辆输入带当前账单集历史补全，允许新建
- [ ] 金额实时预览（整数运算，663 × 6.90 = 4574.70）
- [ ] 「保存这笔」回到对应账单集；「保存并再记一笔」留页并保留账单集与日期

**对账单**
- [ ] 输出格式分段选择（按日期流水表 / 按车辆聚合表），默认流水表，选择按账单集记忆
- [ ] 一键生成；流水表 = 逐笔明细，聚合表 = 双栏（按车名排序，奇数辆右下留空，合计行跨列正确）
- [ ] 标题/说明/供方/出单日期可就地编辑（虚线框 + 铅笔角标，无 hover）；修改按账单集记忆，供方全局记忆
- [ ] 明细数字不可就地改，点按提示「数字来自流水，请到账目页修改后重新生成」
- [ ] 说明文字日期范围取条目最早~最晚日期，可就地手改
- [ ] 列设置跟随所选输出格式显示对应列方案；勾选/改名/排序生效且按「账单集 + 输出格式」分别持久化；双栏版式列配置对左右同时生效

**金额与数据**
- [ ] 金额不落库，全程整数运算；合计 = 各行之和（分毫不差）
- [ ] 金额/单价两位小数，升数最多三位小数去尾零

**导出**
- [ ] PDF 中文正常显示（内嵌字体）；经系统分享面板可发微信、可打印

**边界用例**
- [ ] 空账单集点「一键生成」→ 提示「该账单集暂无流水」，不生成空白 PDF
- [ ] 超长账单集名称/车辆名截断不挤压布局
- [ ] 大金额（十万级以上）列宽不溢出
- [ ] 按车辆聚合表仅 1 台车：右栏留空、合计行正常
- [ ] 排布方式/输出格式反复切换，数据与合计始终一致
- [ ] 预览与 PDF 允许排版细节差异，以 PDF 为准

---

## 9. 后续规划（本期不做，仅记录）

- 数据备份（导出/导入备份文件，应对换机丢数据）
- 对账状态（未出单/已出单/已确认/已结清，数据模型预留 `status` 字段）
- 金额调整（抹零）独立字段
- 账期筛选生成
- 多单位（吨）
